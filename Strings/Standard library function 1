#include <stdio.h>
#include<string.h>

int main()
{   // 'strlen' library gives the length the string without the '\0'.
    char name[] = "THANMAY";
    int length = strlen(name);
    printf("%d",length);
    return 0;
}
