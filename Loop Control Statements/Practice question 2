METHOD - 1(for loop):-
/* print the sum of first n natural numbers.
Also, print them in reverse.
*/
#include <stdio.h>

int main()
{
    int n;
    printf("enter the number: ");
    scanf("%d",&n);
    
    int sum = 0;
    for(int i = n; i >= 1; i--){
        sum += i;
        printf("%d\n",i);
    }
    printf("sum is %d",sum);
    
    return 0;
}
