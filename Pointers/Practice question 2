//Swap 2 numbers.
#include <stdio.h>
int swap(int *a,int *b);

int main()
{
    int x = 3;
    int y = 4;
    swap(&x,&y);
    printf("a= %d & b= %d",x,y);
    
    return 0;
}

int swap(int *a,int *b){
    int t = *a;
    *a = *b;
    *b = t;
    printf("a= %d & b= %d\n",*a,*b);
}
