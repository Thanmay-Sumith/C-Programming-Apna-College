//Call by referance.
#include <stdio.h>
int square(int *n);

int main()
{
    int n = 4;
    square(&n);
    printf("square = %d",n);
    
    return 0;
}

int square(int *n){
    *n = (*n) * (*n);
    printf("square = %d\n",*n);
}
