// Use library function to find square of any number given by the user.
#include <stdio.h>
#include<math.h>

int number(int n);

int main()
{
    int n;
    printf("enter the number: ");
    scanf("%d",&n);
    
    printf("square of the number is %f",pow(n,2));
    return 0;
}
